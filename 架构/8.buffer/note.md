## 编码问题
- 属于global上面的属性,读取文件,写入文件(都是二进制) 从硬盘中读取的数据都存到内存中(默认js是不支持二进制的),buffer可以让字符串和二进制相互转化
- 前端二进制 file(基于blob) blob(前端大数据)  编码
- 编码的发展史 目前都是utf8 以前是ASCII 以前是美国的,大小写26个字母和特殊字符就可以被ASCII包括
  - 一个字节就是8位 每个最大都是1 最大就是255 
- 二进制转换成十进制
- 十进制转换为其他进制
  - ASCII => gbk(node不支持gbk,可以使用第三方模块解析iconv-lite)=> utf-8一个汉字三个字节