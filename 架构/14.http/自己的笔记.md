1. 初始化展示http-server的使用 
   1. npm install -g http-server
   2. 命令行有几个软连 分别在npm路径下的hs 和 http-server
   3. 输入以后按照当前路径启动一个静态服务,直接读取文件
2. 模拟http-server
   1. 可以全局安装
   2. 配一些快捷键 hs 等(代表的是http-server)
   3. --help等帮助命令
   4. gzip 缓存等相关的
3. 模拟逻辑: 
   1. 解析行间命令
   2. 创建服务