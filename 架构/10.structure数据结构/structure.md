## 队列 栈
队列: 先进先出(超市排队)
- 事件环 事件由上到下依次执行,先进先出
栈: 先进后出(混乱的东西,从最外层拿)  
- 浏览器访问记录  (两个栈结构,当后退时,会出栈到一个新的栈中 当访问新的页面时,会清除另一个栈中的结果) 可以用词法分析, 分析标签的闭合
- 线性结构(一条线): 队列 栈 链表

## 链表 树
链表: 一节一节的绳子头尾相连 头是head 尾是tail 下一个指针是next 上一个指针是prev 

## 数组添加元素 o(n)整体循环一遍
## 二分查找 o(logn)折半查找