// 问题收集处
- Object.create 我记得是创建一个对象,把新创建的对象的原型指向传入对象的原型,其实最初的原型是不改动,但是如果不xx.__proto__.xx属性的改动,是无法改动最初的属性. 但是老师的介绍是每次都是一个新对象,属性互相不关联,就不理解. 在16.createContext和 this.context里都有介绍. 视频:  2020年4期/5期的架构:任务54：2.ctx创建的机制17分钟
- ctx中间件挂载new 一个promise,每次发送的请求挂载到ctx.request.body上.
  - 解题: 感觉就是每次来请求,都把请求的数据提前拦截. 之前疑惑点是不同请求如何区分,后来明白了,是每次请求都会调用这些中间件,所以在第一个中间件的时候就拦截数据,后面使用即可