1. 减少http请求,精灵图
2. 图片懒加载
3. dns: 预解析,移动端: meta标签 http-equiv='x-dns-preFeach-control' content='on'
4. cdn: 多个文件统一打包成为一个文件下载
5. ssr: 
    是否需要: 如果只是一个仪表盘,则不需要.但是如果是一个客户的页面,甚至是百度的初始化搜索页面,那肯定是需要的.
    初级使用: 
        1. npm vue-server-render
        2. 将vue实例渲染为html
    优点:  
        1. 利于seo优化,因为谷歌/bing 都是同步js解析,如果页面还需要ajax请求,那么建议使用ssr优化,代码放在服务器端
        2. time-to-content 更快的(内容到达时间): 特别是对于缓慢的网络情况或运行缓慢的设备。无需等待所有的 JavaScript 都完成下载并执行，才显示服务器渲染的标记，所以你的用户将会更快速地看到完整渲染的页面。通常可以产生更好的用户体验，并且对于那些「内容到达时间(time-to-content) 与转化率直接相关」的应用程序而言，服务器端渲染 (SSR) 至关重要。
    缺点:  
        1. 一些代码库,需要特殊处理才能在应用程序中使用
        2. 与可以部署在任何静态服务器上的完全静态单页面应用程序(spa)不同,服务器渲染应用程序需要处于Node.js server中(因为node可以解析JavaScript)
        3. 更多的服务器负载: 在Node.js中渲染完整的应用程序,显然比静态文件的服务更加大量占用CPU资源(cpu密集),如果你在高流量资源下使用,请准备相应的服务器负载,并明智的选择缓存策略


