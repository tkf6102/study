"use strict";
/* interface objType {
  num: number;
  str: string
}

let obj:objType = {
  num:1,
  str:'2'
} */
var l = {
    jj: [1, 2, 3] // 必须是List里面规定好的数据
    // jj:{}  必须是按照interface里面对应jj的属性来设定
};
console.log(l.jj);
